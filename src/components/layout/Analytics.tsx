'use client';

import { useEffect } from 'react';

export default function Analytics() {
  useEffect(() => {
    // Simple analytics setup - can be enhanced later
    console.log('Analytics component loaded');
  }, []);

  return null;
} 